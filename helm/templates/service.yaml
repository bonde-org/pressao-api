apiVersion: v1
kind: Service
metadata:
  name: {{ include "pressao-api.fullname" . }}
  labels:
    {{- include "pressao-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pressao-api.selectorLabels" . | nindent 4 }}
